kind = "web"
previewPath = "/"
title = "AI Hiring Agent"
version = "1.0.0"
id = "artifacts/hiring-agent"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22151

[services.development]
run = "pnpm --filter @workspace/hiring-agent run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/hiring-agent", "run", "build" ]
publicDir = "artifacts/hiring-agent/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22151"
BASE_PATH = "/"
